{{define "title"}}TailScribe - Log In"{{end}}

{{define "main"}}
<div class="mdl-card mdl-shadow--2dp">
    <h1>Welcome back!</h1>

    <p>Log in to your account</p>

    <form method="POST" action="/login">
        <input name="email" />
        <input name="password" />

        <button>Login</button>
    </form>
</div>
{{end}}